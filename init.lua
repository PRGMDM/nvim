require("core.options")
require("core.keymaps")
require("core.autocmds")
require("core.lazy")
-- require("stolen.difftool").setup({})

-- TODO: really need to decide which plugins can be lazy loaded. Might be a good idea to group them by file type 
